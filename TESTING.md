# 手工验收清单（0.4.1）

本清单对应四个功能：图片/剪贴板取色、AI 配色与 Agent 集成、PI-Desktop 主题,以及**当前版本不适用**的全局快捷键（0.4.1 起摘除,原因见第 1 节）。
每项都写了**预期结果**和**不对时看哪里**。逐项打勾即可。

> 自动化部分（107 条 `node --test`、devkit `check`、无头 Chromium 交互、宿主真消毒器、级联探针）
> 已在开发侧跑通；本清单只覆盖**必须由真机宿主确认**的部分。

---

## 0. 先把环境对齐（不做这步，后面全是误判）

- [ ] **确认宿主加载的目录**：宿主里打开插件详情，看它记录的路径；也可以直接读
      `~/.pi-desktop/plugins/registry.json`，其中 `path` 字段是宿主记忆的加载位置。
      注意：开发插件是按原路径加载的，宿主不会复制副本（`plugins/cache/` 里只有下载缓存）。
- [ ] **确认版本是 0.4.1**：插件列表应显示 `0.4.1`。或看导出页底部有没有
      「PI-Desktop 主题」区块——那是 0.4.0 的标志。
- [ ] **确认宿主够新**：`pi.themes` / `pi.app.setTheme`（ADR 0260）**还没进任何发布版**，
      所以要跑**从 main 起的构建**（本机 `/home/gargantua/code/PI-Desktop` 检出就是）。
      旧宿主上主题区块会显示"当前宿主没有运行期主题 API（ADR 0260），需要更新 PI-Desktop"，
      其余功能照常——看到这句就是宿主太旧，不是插件坏了。
- [ ] **重新加载并授权**：0.4.0 新增了 1 个权限 `ui.theme`（生成并安装主题，低风险）。
      按宿主规则，**权限变更会中断热重载并要求重新授权**。
      若宿主没有自动提示，就重新走一次「插件 → Load development plugin」，或把插件
      禁用再启用。`agent.tool.register` 与 `agent.prompt.inject` 属于高风险权限，会有额外确认。
- [ ] **重开面板**：renderer 的改动在同一次会话里可能被 Chromium 缓存，
      关掉面板再打开（必要时重启应用）才能看到新界面。
- [ ] **确认权限真的授予了**：插件详情里的权限列表应包含全部 11 项（含 `ui.theme`）。
      漏授时宿主会拒绝调用，界面会显示
      「缺少权限: fs.read — 请重新加载插件（或禁用后重新启用）并授予新权限」
      （0.3.0 之前这句是 Electron 的原始报错 `Error invoking remote method
      'pi-plugin-panel-invoke': Error: missing permission: fs.read`）。
      看到它就说明是授权问题，不是功能问题——回到上一步重新加载并授权。
      注意：**热重载不能扩大权限**，只改文件是不够的，必须让宿主重新走一次授权。

---

## 1. 全局快捷键 —— 0.4.1 起不再随版本发布（跳过本节）

`Alt+Shift+C` 需要 `keyboard.globalShortcut`，而该权限随宿主 PR #409 落地、至今没进任何发布版：插件中心按未知权限拦住了提交（MAN013），任何可安装的宿主也给不出这个权限。所以 0.4.1 把它从 manifest 里摘掉了，**这一节现在只适用于宿主发布之后**；恢复步骤见 [PLAN.md](./PLAN.md) 阶段 1 的「0.4.1 处理」。

- [ ] 现在能做的替代验证：命令面板里执行「Color Picker: Open」，
      **预期**：弹出**独立面板窗**（命令打不开工作面板视图——宿主没有 `openView` API，这是设计如此）。

宿主发布 PR #409 之后，本节恢复为原来的三件事：切到别的应用按 `Alt+Shift+C` 能唤起面板、插件设置里能看到并改绑这条快捷键、换键后新键生效。日志里若出现 `was refused, using Ctrl+Alt+C` 表示首选键被占已自动降级，`refused: SHORTCUT_CONFLICT` 表示三个候选都被占，`failed: UNSUPPORTED` 表示宿主太旧。

---

## 2. 图片 / 剪贴板取色

### 2a. 从文件夹取色

- [ ] 面板里点「从图片取色」→ 覆盖层打开，来源默认「文件夹」。
- [ ] 点「选择文件夹…」→ **预期**：宿主弹出原生目录选择器。
- [ ] 选一个装有图片的目录 → **预期**：下方列出其中的图片
      （只列 PNG / JPEG / WebP / GIF / BMP / AVIF / SVG；`.md` 之类不出现）。
      超过 1000 项时会提示"只列出了前 1000 项"。
- [ ] 点其中一张图 → **预期**：图片铺满舞台、放大镜跟随光标、读数实时变化。
- [ ] 把光标移到某个像素上，按方向键 → **预期**：1 像素步进，读数跟着变；
      按住 Shift 一次 10 像素；到图片边缘会停住（不会越界）。
- [ ] 在图上点一下 → **预期**：取色层关闭，面板当前色变成刚取的颜色，
      上方四个记法同步更新。
- [ ] 再打开取色层，按 `Esc` → **预期**：直接关闭，当前色不变。
- [ ] 用一张**已知纯色**的图（比如整张 `#FF0000`）重复上面两步，
      确认读数与面板显示的就是 `#ff0000`（这是判定"取样取对了像素"的最快方法）。

**负路径（都应有明确文案，不能静默失败）**

- [ ] 取消目录选择 → 提示"选择一个文件夹，这里会列出其中的图片。"
- [ ] 选一个没有图片的目录 → 提示"这个文件夹里没有 PNG / JPEG / …"。
- [ ] 点一张 **大于 5 MiB** 的图 → 提示"超过宿主 5 MiB 的预览上限，请先缩小图片。"
      （这是宿主的硬限制，取色层不会崩、当前色也不变。）
- [ ] 选一张损坏/改名的非图片文件 → 提示无法预览或无法解码。

### 2b. 从剪贴板取色

- [ ] **先做这一步**：在对话输入框里**粘贴一张截图**（Ctrl+V）。
      宿主的剪贴板历史只记录**粘贴进会话**的图片，不是系统剪贴板里的所有内容——
      直接在别的应用里复制截图是看不到的。
- [ ] 面板取色层切到「剪贴板」→ **预期**：直接载入最新那张图。
- [ ] 之后的操作与 2a 相同。
- [ ] 历史里没有图片时切到「剪贴板」→ **预期**：提示"剪贴板历史里没有图片——…"。

> 说明：宿主没有剪贴板变更事件，所以取色层只在**打开的那一刻**读一次历史；
> 期间再粘贴新图，需要重新打开取色层。

---

## 3. AI 配色

### 3a. 前置条件

- [ ] 设置里至少配置好一个**可用**的提供商与模型（宿主的 `models.list` 只返回就绪的那些；
      没配置时生成按钮会是禁用状态并提示"没有可用的模型——请先在设置里配置提供商。"）。
- [ ] 打开配色标签页，滚到底部的「AI 配色」区块。

### 3b. 生成与使用

- [ ] 模型下拉 → **预期**：列出你配置好的模型，默认选中宿主的默认模型
      （第一项是"宿主默认"，表示交给宿主决定）。
- [ ] 选一个具体模型 → 关掉面板再打开 → **预期**：仍然是刚才那个模型（选择已持久化）。
- [ ] 输入风格描述（例如"科技感、深色、偏紫"），点「生成」。
      **预期**：状态行变"生成中…"→ 完成后显示"已生成 · N tokens"，下方出现
      带名称的色带（名称应是中文，与界面语言一致）。
- [ ] 点结果里的某一格 → **预期**：它成为当前色（上方头部与四种记法同步）。
- [ ] 再点「整套用作导出」→ **预期**：跳到导出页，导出范围自动变成"最近的色板"，
      预览里是按颜色名生成的 CSS 变量（如 `--color-deep-water: #0b1f33;`）。
- [ ] 换一种格式（Tailwind v4 / v3 / JSON）确认都能导出。

### 3c. 限制与失败路径（这些都应该有明确文案，不是白屏或静默）

- [ ] **连续快速点 9 次以上生成** → 提示"一分钟内调用次数已达上限（宿主限制 8 次）…"。
      注意：**失败的调用也算次数**，所以报限流后要等一分钟。
- [ ] 用一个响应很慢的模型 → 提示"面板等待上限 30 秒…"。
      （面板→主进程的转发通道是 30 秒，而 `agent.complete` 自身允许 90 秒，
      所以在面板里要快；这条限制写在 README 里。）
- [ ] 如果要验证"模型返回散文"的情况：用一个不太听话的小模型，
      或临时把提示词改坏——预期会显示"模型没有返回可用的配色："并把模型原文贴出来。

### 3d. Agent 集成

- [ ] **不要**在 Plan / Goal 模式下测试——宿主对所有插件的工具都在这两个模式禁用。
- [ ] 在对话里说："用 suggest_palette 给我一套深色、科技感、偏紫的配色"。
      **预期**：Agent 调用工具并返回带名称的十六进制颜色。
- [ ] 说"用 #3b82f6 作为主色，再加四个搭配色" → 预期工具收到 `base` 参数并返回 5 色。
- [ ] 反问 Agent"你知道这个配色工具能做什么吗" → 理想情况下它会引用 skill 文档里的说明
      （skill 是宿主按需加载的，不一定每次都读到）。

---

## 4. PI-Desktop 主题（0.4.0 新增）

这一项会**真的改变你的应用外观**，所以每一步都给了还原办法。面板里的入口在**导出页最底部**。

### 4a. 生成并应用

- [ ] 先在色板里选一个有点饱和度的颜色（比如 Tailwind 的 `blue-500`），
      切到导出页 → 滚到底部「PI-Desktop 主题」。
      **预期**：底色下拉默认跟当前应用明暗一致；右侧一条预览色带（7 格，从左到右由深到浅，
      最后一格是强调色）；状态行只有主题名（如「蓝 · 深色」），悬停可以看到强调色对比度。
- [ ] 点「生成并应用」。
      **预期**：整个应用外壳**立刻**换色（侧栏、编辑器背景、输入框、设置页都跟着变），
      不需要重启、不需要重载插件。按钮变成「重新生成」，状态行多出"已应用"。
- [ ] 打开 设置 → 通用 → 主题 → **预期**：列表里出现一个「<色族> · 深色」的条目，
      副标题标明来自本插件；它是当前选中项。
- [ ] 在宿主的主题选择器里切回「系统」或「浅色」，再切回刚才那条插件主题 →
      **预期**：来回切换都即时生效。
- [ ] 回到面板，把底色改成「浅色」，点「生成并应用」→ **预期**：应用变浅色主题，
      面板状态行显示的是浅色那一套；深色那套仍留在宿主主题列表里（两个槽位共存）。
- [ ] 再点「重新生成」（同一个底色、换一个当前色）→
      **预期**：宿主主题列表里**不会多出一条**，是同一个槽位被覆盖（这是设计如此）。

### 4b. 还原与卸载

- [ ] 点「还原」→ **预期**：回到你安装之前用的那套（安装时记下的，通常是"系统"）；
      主题仍在列表里（卸载与还原是两件事），此时状态行从"已应用"变成"已安装"。
- [ ] 点「卸载」→ **预期**：宿主主题列表里那条消失；如果当时正用着它，应用会自动回到
      之前那套（不会停在一半）。
- [ ] 重新点「生成并应用」再「卸载」，确认重复操作不会报错。

### 4c. 持久化（这一条最容易被忽略）

- [ ] 生成并应用一个主题后，**完全退出并重启 PI-Desktop**。
      **预期**：启动后主题仍然生效（宿主的注册表是"插件加载时重建"的——
      插件每次加载都会把自己存下的 CSS 重新注册一遍，所以禁用再启用、重启都不会丢）。
- [ ] 把插件**禁用再启用** → **预期**：主题自己回来（宿主记得偏好，插件重新注册）。
- [ ] 在宿主的主题选择器里换成「浅色」，然后禁用插件 → **预期**：应用干净地回到浅色，
      不会卡在半套配色上。

### 4d. 边界与负路径

- [ ] 选一个**接近纯黑**的颜色生成深色主题 → **预期**：能生成，强调色会自动提亮到可读
      （状态行里的对比度应 ≥ 4.5:1），不会生成一个看不见的强调色。
- [ ] 选一个**纯灰**（如 `#808080`）→ **预期**：主题名是「中性 · 深色」，整套是灰阶，没有色偏。
- [ ] 点「复制 CSS」→ **预期**：剪贴板里是一段 `:root[data-plugin-theme="plugin:…"][data-theme] { … }`，
      可以直接贴进别的应用日志或分享。
- [ ] 切到 English 再走一遍 → **预期**：区块文案与主题名都是英文（如 `Blue · Dark`）。
- [ ] 授予权限前的状态（如果宿主没有自动要求重新授权）：点「生成并应用」→
      **预期**：显示"缺少权限: ui.theme — 请重新加载插件（或禁用后重新启用）并授予新权限"，
      而不是 Electron 的原始报错。
- [ ] **宿主不支持时的形态**：整块只有「底色」和「复制 CSS」可用（预览色带仍然跟着底色变），
      说明行变成"当前宿主缺少运行期主题 API（ADR 0260），需更新 PI-Desktop。"
      被禁用的按钮与下拉都是半透明的——不会有"看起来能点但点不动"的控件。
      （0.4.0 初版这里把底色下拉也一起禁用了，偏偏面板自己的样式盖掉了系统的禁用外观，
      所以看起来完全正常却点不动。）

> 说明：主题只覆盖**应用外壳**，本插件面板自己的配色不受影响（面板是独立沙箱文档）；
> 启动瞬间的原生窗口底色仍是宿主给当前明暗的默认值，运行期改不到——这两点写在 README 的已知限制里。

---

## 5. 顺手回归一下旧功能（本次改动碰过样式）

- [ ] 色板页：Tailwind / Material / 预设 / 渐变四个来源都能翻，点色块能选中。
- [ ] **色块选中环**：分别选一个很浅的色块和一个很深的色块，
      两者的环都应清晰可见（浅色是细黑线、深色是白内环+黑外环），且不刺眼。
- [ ] 导出页两个下拉框：打开时弹层**在控件下方**、圆角面板样式与面板一致、跟随明暗主题；
      键盘 `ArrowDown` / `Enter` / `Esc` 都能用。
- [ ] 对比度页、导出页文案与 AI / 主题两个区块在**中英文**下都不溢出
      （宿主语言切到 English 再看一遍；主题区块还要确认长主题名与权限报错不会把面板撑宽）。

---

## 6. 需要反馈给我的信息

如果某一项没通过，请给我这三样，我能直接定位：

1. **宿主构建**：是从 main 起的构建，还是那份 `settings-select-controls` worktree？
   （主题能不能工作取决于宿主是否已含 ADR 0260；全局快捷键在 0.4.1 里已摘除，见第 1 节。）
2. **权限是否已授予**：插件详情里的权限列表截图或抄录。
3. **日志中按 `color-picker` 过滤出来的那几行**，以及界面上实际显示的文案。
